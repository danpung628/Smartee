# 추적되는 기본값(placeholder). 실제 비밀 값은 gitignore 대상인 secrets.properties 에 둔다.
# fresh clone 이 secrets.properties 없이도 컴파일되도록 보장하는 용도(런타임 동작은 실제 키 필요).
NAVER_CLIENT_ID = DEFAULT_CLIENT_ID
MAPS_API_KEY = YOUR_MAPS_API_KEY
DEFAULT_WEB_CLIENT_ID = YOUR_WEB_CLIENT_ID
